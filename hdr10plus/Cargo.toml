[package]
name = "hdr10plus"
version = "2.0.0"
authors = ["quietvoid"]
edition = "2021"
rust-version = "1.56.1"
license = "MIT"
description = "HDR10+ metadata parsing and writing"
repository = "https://github.com/quietvoid/hdr10plus_tool/tree/master/hdr10plus"

[dependencies]
bitvec_helpers = { version = "3.1.1", default-features = false, features = ["bitstream-io"] }
anyhow = "1.0.68"
serde = { version = "1.0.152", features = ["derive"], optional = true }
serde_json = { version = "1.0.91", features = ["preserve_order"], optional = true }
hevc_parser = { version = "0.5.2", optional = true }

[features]
hevc = ["hevc_parser"]
json = ["serde", "serde_json"]

[package.metadata.docs.rs]
all-features = true
