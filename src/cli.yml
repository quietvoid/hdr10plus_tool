name: HDR10+ Parser
version: "0.2.3"
author: quietvoid
about: Parses HEVC dynamic metadata
args:
    - input:
        help: Sets the input file to use
        required: true
        index: 1
    - input:
        help: Sets the input file to use
        short: i
        long: input
        takes_value: true
        required: true
    - output:
        help: Sets the output JSON file to use
        index: 2
    - output:
        help: Sets the output JSON file to use
        short: o
        long: output
        takes_value: true
    - verify:
        long: verify
        help: Checks if input file contains dynamic metadata