name: HDR10+ Parser
version: "0.2.3"
author: quietvoid
about: Parses HEVC dynamic metadata
args:
    - INPUT:
        help: Sets the input file to use
        required: true
        index: 1
    - INPUT:
        help: Sets the input file to use
        short: i
        long: input
        takes_value: true
        required: true
    - OUTPUT:
        help: Sets the output JSON file to use
        index: 2
    - OUTPUT:
        help: Sets the output JSON file to use
        short: o
        long: output
        takes_value: true
    - verify:
        long: verify
        help: Only checks if dynamic metadata is found.